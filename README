New system checklist:

1) Install tmux
2) Install vim
3) Install zsh
4) Install oh-my-zsh
5) clone this repo into ~/dotfiles
6) install vundle
7) install pathogen
   a)  install vim-racket package
8) Install antigen
9) ~/dotfiles/install_script
10) vim bundle install
